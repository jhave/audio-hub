import ExperienceClient from "./ExperienceClient"

export const metadata = {
  title: "Digital Humanities Archive View — 171 days (Music Created in Suno) — Glia",
}

export default function Page() {
  return <ExperienceClient />
}
